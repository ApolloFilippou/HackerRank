def miniMaxSum(arr):
    maxnum = max(arr)
    minnum = min(arr)
    arr2 = arr.copy()
    arr.remove(maxnum)
    arr2.remove(minnum)
    big = sum(arr)
    small = sum(arr2)
    print(big, small)
if __name__ == '__main__':
    arr = list(map(int, input().rstrip().split()))

    miniMaxSum(arr)
