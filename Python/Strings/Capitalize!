# Complete the solve function below.
def solve(s):
    if s[0]==s[0].lower():
        s = s[0].upper()+s[1:]
    for i in range(1,len(s)):
        if s[i-1]==' ':
            s = s[:i]+s[i].upper()+s[i+1:]
    return s
