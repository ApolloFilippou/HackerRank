import numpy

N, M = [int(x) for x in input().split()]
arr = []
for _ in range(N):
    arr.append([int(x) for x in input().split()])

arr = numpy.array(arr)
print(numpy.transpose(arr))
print(arr.flatten())
